# 🚀 TenChat NeuroBooster v1.0

**Self-Hosted сервис для автоматизации продвижения в социальной сети TenChat**

Аналог сервиса `ten5.ru`, но для личного использования с полным контролем над процессом.

---

## 📋 Возможности

- ✅ **Управление несколькими аккаунтами** - добавляйте и управляйте любым количеством TenChat аккаунтов
- 🔒 **Безопасная авторизация** - через JSON cookies (без передачи паролей)
- 🌐 **Прокси для каждого аккаунта** - строгая привязка 1 аккаунт = 1 прокси
- 🤖 **AI генерация контента** - автоматическое создание статей через OpenAI-compatible API
- 🔥 **Режим прогрева** - автоматический лайкинг ленты для повышения активности
- ⏱️ **Эмуляция человека** - случайные паузы между действиями (60-180 сек)
- 🛡️ **Защита от банов** - дневные лимиты (50 лайков, 20 подписок, 2 поста)
- 📊 **Детальная статистика** - логи всех действий и визуализация в UI
- 🐳 **Docker Compose** - запуск одной командой

---

## 🛠 Технологии

- **Backend:** FastAPI + SQLite
- **Frontend:** Streamlit
- **HTTP Client:** httpx (HTTP/2 support)
- **AI:** OpenAI-compatible API (поддержка OpenRouter, OpenAI, локальных моделей)
- **Deploy:** Docker Compose

---

## 📦 Установка и Запуск

### Вариант 1: Docker Compose (Рекомендуется)

1. **Клонируйте репозиторий:**
```bash
git clone <repository_url>
cd TenChat_NeuroBooster_v1
```

2. **Создайте файл .env:**
```bash
cp .env.example .env
```

3. **Отредактируйте .env и укажите API ключ:**
```env
AI_BASE_URL=https://openrouter.ai/api/v1
AI_API_KEY=sk-or-v1-ваш-ключ-здесь
```

4. **Запустите сервис:**
```bash
docker-compose up -d
```

5. **Откройте UI в браузере:**
```
http://localhost:8501
```

6. **API документация доступна по адресу:**
```
http://localhost:8000/docs
```

### Вариант 2: Локальный запуск (Разработка)

1. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Создайте .env файл:**
```bash
cp .env.example .env
# Отредактируйте .env
```

4. **Запустите FastAPI backend:**
```bash
python -m app.main
```

5. **В новом терминале запустите Streamlit UI:**
```bash
streamlit run ui/streamlit_app.py
```

---

## 🔧 Настройка

### 1. Покупка Прокси

Рекомендуется использовать **Server IPv4 Proxy (Russia)** от провайдера [px6.me](https://px6.me)

**Инструкция:**
1. Перейдите на [px6.me](https://px6.me)
2. Выберите "Server IPv4 Proxy"
3. Страна: **Russia**
4. Тип: **HTTP/HTTPS**
5. Период: по необходимости (рекомендуется 1 месяц)
6. После покупки вы получите данные в формате: `ip:port:login:password`

**ВАЖНО:**
- Для каждого аккаунта используйте **отдельный прокси**
- Не используйте один прокси на несколько аккаунтов
- Не используйте мобильные прокси для автоматизации

### 2. Получение Cookies из браузера

**Способ 1: Расширение EditThisCookie (Chrome)**

1. Установите [EditThisCookie](https://chrome.google.com/webstore/detail/editthiscookie/)
2. Откройте [tenchat.ru](https://tenchat.ru) и авторизуйтесь
3. Нажмите на иконку EditThisCookie
4. Нажмите "Export" (иконка экспорта)
5. Сохраните JSON в файл `cookies.json`

**Способ 2: Расширение J2TEAM Cookies (Chrome/Edge)**

1. Установите [J2TEAM Cookies](https://chrome.google.com/webstore/detail/j2team-cookies/)
2. Откройте [tenchat.ru](https://tenchat.ru) и авторизуйтесь
3. Нажмите на иконку расширения
4. Нажмите "Export" → "JSON"
5. Сохраните файл

**Способ 3: Вручную (DevTools)**

1. Откройте [tenchat.ru](https://tenchat.ru) и авторизуйтесь
2. Нажмите F12 (DevTools)
3. Вкладка Application → Cookies → tenchat.ru
4. Скопируйте cookies в формат JSON:
```json
[
  {
    "name": "sessionid",
    "value": "ваше_значение",
    "domain": ".tenchat.ru"
  }
]
```

### 3. Настройка AI API

Сервис поддерживает любые OpenAI-compatible API.

#### Вариант A: OpenRouter (Рекомендуется)

1. Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai/)
2. Пополните баланс ($5-10 хватит на месяц)
3. Создайте API ключ
4. В `.env` укажите:
```env
AI_BASE_URL=https://openrouter.ai/api/v1
AI_API_KEY=sk-or-v1-ваш-ключ
```

**Используемые модели:**
- `openai/gpt-4o-mini` - комментарии (~$0.15 на 1M токенов)
- `anthropic/claude-3.5-haiku` - статьи (~$0.80 на 1M токенов)
- `deepseek/deepseek-chat` - аналитика (~$0.14 на 1M токенов)

#### Вариант B: OpenAI напрямую

```env
AI_BASE_URL=https://api.openai.com/v1
AI_API_KEY=sk-ваш-openai-ключ
AI_MODEL_COMMENTS=gpt-4o-mini
AI_MODEL_ARTICLES=gpt-4o
```

#### Вариант C: Локальная модель (Ollama/LM Studio)

```env
AI_BASE_URL=http://localhost:1234/v1
AI_API_KEY=not-needed
AI_MODEL_COMMENTS=llama3.2
AI_MODEL_ARTICLES=llama3.2
```

---

## 📖 Использование

### Добавление аккаунта

1. Откройте UI: `http://localhost:8501`
2. Вкладка **"Аккаунты"**
3. Нажмите "➕ Добавить Аккаунт"
4. Заполните форму:
   - **Название:** любое имя (например: "Мой TenChat")
   - **Cookies:** загрузите файл `cookies.json`
   - **Прокси:** введите в формате `ip:port:login:password`
5. Нажмите **"Добавить и Проверить"**

Система автоматически проверит валидность cookies и прокси.

### Запуск задачи "Прогрев"

1. Вкладка **"Задачи"**
2. Выберите аккаунты (можно несколько)
3. Выберите **"🔥 Прогрев (Лайкинг ленты)"**
4. Настройте параметры:
   - **Количество лайков:** 5-50 (рекомендуется 10-20)
   - **Тип ленты:** Все / Бизнес / IT / Маркетинг
5. Нажмите **"🚀 Запустить Задачи"**

### Запуск задачи "AI Постинг"

1. Вкладка **"Задачи"**
2. Выберите аккаунт
3. Выберите **"✍️ AI Постинг"**
4. Введите тему статьи, например:
   ```
   Как использовать искусственный интеллект для автоматизации маркетинга в 2025 году
   ```
5. Выберите стиль написания
6. Нажмите **"🚀 Запустить Задачи"**

AI сгенерирует статью с заголовком, структурой и хештегами, после чего автоматически опубликует её.

### Просмотр логов

1. Вкладка **"Логи"**
2. Просмотрите все действия:
   - Лайки
   - Подписки
   - Публикации
3. Используйте фильтры по аккаунту и типу действия
4. Смотрите статистику успешности

---

## ⚙️ Конфигурация

### Настройка лимитов безопасности

Отредактируйте `.env`:

```env
# Дневные лимиты
DAILY_LIMIT_LIKES=50        # Максимум лайков в день
DAILY_LIMIT_FOLLOWS=20      # Максимум подписок в день
DAILY_LIMIT_POSTS=2         # Максимум постов в день

# Паузы между действиями (секунды)
MIN_ACTION_DELAY=60         # Минимальная пауза
MAX_ACTION_DELAY=180        # Максимальная пауза
```

**Рекомендации:**
- Не увеличивайте лимиты выше указанных значений
- Для новых аккаунтов используйте более консервативные значения
- Первую неделю делайте не более 20 лайков в день

### Настройка AI моделей

```env
AI_MODEL_COMMENTS=openai/gpt-4o-mini          # Быстрая модель для комментариев
AI_MODEL_ARTICLES=anthropic/claude-3.5-haiku  # Качественная модель для статей
AI_MODEL_ANALYTICS=deepseek/deepseek-chat     # Экономичная модель для аналитики
```

---

## 🐳 Docker команды

```bash
# Запустить сервисы
docker-compose up -d

# Остановить сервисы
docker-compose down

# Посмотреть логи
docker-compose logs -f

# Посмотреть логи только UI
docker-compose logs -f ui

# Посмотреть логи только API
docker-compose logs -f api

# Перезапустить сервисы
docker-compose restart

# Пересобрать образы
docker-compose build --no-cache

# Удалить всё (включая данные!)
docker-compose down -v
```

---

## 📊 Структура проекта

```
TenChat_NeuroBooster_v1/
├── app/
│   ├── models/              # SQLAlchemy модели
│   │   └── database.py
│   ├── services/            # Бизнес-логика
│   │   ├── tenchat_client.py    # TenChat API wrapper
│   │   ├── ai_generator.py      # AI генератор контента
│   │   └── task_executor.py     # Исполнитель задач
│   ├── utils/               # Утилиты
│   │   ├── cookies_parser.py
│   │   ├── proxy_handler.py
│   │   └── user_agent_generator.py
│   └── main.py              # FastAPI приложение
├── ui/
│   └── streamlit_app.py     # Streamlit UI
├── config/
│   └── settings.py          # Настройки
├── data/                    # База данных (создается автоматически)
├── logs/                    # Логи (создается автоматически)
├── docker-compose.yml       # Docker Compose конфигурация
├── Dockerfile               # Docker образ
├── requirements.txt         # Python зависимости
├── .env.example             # Пример настроек
└── README.md                # Документация
```

---

## ⚠️ Безопасность и Ограничения

### Рекомендации по безопасности

1. **Прокси:**
   - Используйте **только серверные IPv4 прокси**
   - Один прокси = один аккаунт
   - Меняйте прокси раз в месяц

2. **Лимиты:**
   - Не превышайте дневные лимиты
   - Для новых аккаунтов: первую неделю не более 10-20 действий в день
   - Делайте перерывы (2-3 дня без активности в месяц)

3. **Cookies:**
   - Обновляйте cookies раз в 2 недели
   - Не используйте cookies с разных IP
   - При статусе "cookies_expired" - сразу обновите cookies

4. **Контент:**
   - Проверяйте AI-сгенерированный контент перед публикацией
   - Избегайте спамных тем
   - Публикуйте не более 1-2 статей в день

### Что делать при бане

1. **Статус "error" или "captcha":**
   - Остановите все задачи для этого аккаунта
   - Зайдите в TenChat вручную через тот же прокси
   - Решите капчу (если есть)
   - Обновите cookies

2. **Полный бан:**
   - К сожалению, при постоянном бане восстановить доступ сложно
   - Создавайте новые аккаунты осторожно
   - Используйте новые прокси

---

## 🐛 Решение проблем

### API не запускается

```bash
# Проверьте логи
docker-compose logs api

# Убедитесь что порт 8000 свободен
netstat -an | grep 8000

# Пересоздайте контейнеры
docker-compose down
docker-compose up -d --force-recreate
```

### UI не подключается к API

1. Проверьте что API работает: `http://localhost:8000/`
2. Проверьте логи UI: `docker-compose logs ui`
3. Убедитесь что оба контейнера в одной сети: `docker network ls`

### Cookies не работают

1. Убедитесь что вы экспортировали cookies в правильном формате (JSON array)
2. Проверьте что cookies не истекли (зайдите на tenchat.ru)
3. Попробуйте экспортировать заново

### Прокси не работает

1. Проверьте формат: `ip:port:login:password`
2. Убедитесь что прокси активен (протестируйте в браузере)
3. Проверьте тип прокси (должен быть HTTP/HTTPS)

---

## 💡 FAQ

**Q: Сколько стоит запуск сервиса?**
A:
- VPS: $5-10/месяц (рекомендуется 2GB RAM)
- Прокси: $3-5/месяц за 1 IP
- AI API: $5-10/месяц (при умеренном использовании)

**Q: Можно ли запустить на Windows?**
A: Да, через Docker Desktop или WSL2. Также работает нативно с Python 3.11+.

**Q: Безопасно ли это?**
A: При соблюдении всех рекомендаций (лимиты, прокси, паузы) риски минимальны, но полностью исключить их нельзя.

**Q: Можно ли использовать несколько аккаунтов?**
A: Да, сервис поддерживает неограниченное количество аккаунтов. Главное - отдельный прокси для каждого.

**Q: Нужно ли держать компьютер включенным?**
A: Рекомендуется запускать на VPS сервере для круглосуточной работы.

---

## 📝 Лицензия

Проект создан исключительно в образовательных целях. Используйте на свой риск.

**Disclaimer:**
- Автор не несет ответственности за блокировки аккаунтов
- Используйте сервис в соответствии с правилами TenChat
- Не используйте для спама или нарушения ToS

---

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте раздел "Решение проблем"
2. Изучите логи: `docker-compose logs`
3. Убедитесь что используете последнюю версию

---

**Версия:** 1.0.0
**Дата:** 2025
**Стек:** Python 3.11+ | FastAPI | Streamlit | SQLite | Docker
